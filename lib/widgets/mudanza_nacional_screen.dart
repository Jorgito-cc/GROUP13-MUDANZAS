import 'package:flutter/material.dart';

class MudanzaNacionalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mudanza Nacional')),
      body: const Center(
        child: Text('Contenido de la Mudanza Nacional'),
      ),
    );
  }
}
