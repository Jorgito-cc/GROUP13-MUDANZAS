import 'package:flutter/material.dart';

class Servicio1Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Servicio 1')),
      body: const Center(
        child: Text('Contenido de Servicio 1'),
      ),
    );
  }
}
