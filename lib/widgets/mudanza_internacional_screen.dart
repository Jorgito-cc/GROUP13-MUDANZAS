import 'package:flutter/material.dart';

class MudanzaInternacionalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mudanza Internacional')),
      body: const Center(
        child: Text('Contenido de la Mudanza Internacional'),
      ),
    );
  }
}
